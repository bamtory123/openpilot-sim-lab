# Absolute WSL path to the instrumented fork. Do not place credentials here.
OPENPILOT_ROOT=/home/hyunsung/src/openpilot
# Optional: permit a run with uncommitted code; the manifest records it.
# SIMLAB_ALLOW_DIRTY=1
